# Supabase Seed Data

The V1 repository ships with a transparent development dataset in `content/samples/opportunities.sample.json`.

Do not import those sample records into production as verified content. Production opportunity rows must be created through the content workflow in `docs/content-operations.md` after official-source review.
